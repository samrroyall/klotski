interface Globals {
  numRows: number;
  numCols: number;
  winningRow: number;
  winningCol: number;
  cellSize: number; // in rem
}

export const globals: Globals = {
  numRows: 5,
  numCols: 4,
  winningRow: 3,
  winningCol: 1,
  cellSize: 8,
};
